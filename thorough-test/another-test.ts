// More AI slop patterns for comprehensive testing

// This code hopefully works as expected
function anotherExample(): any {
  // Apparently this function does something important
  const items: any[] = [];
  
  // Probably this loop will work fine
  for (let i = 0; i < 10; i++) {
    items.push(i);
  }
  
  return items; // of course this returns the right thing
}

// Assumption-based comment
// Presumably this function processes data correctly
function processData(data: any): any {
  return data; // naturally this is the right approach
}

// Vibe coding: unnecessary IIFE wrapper
const wrappedResult = (async () => {
  return "some value";
})();

// Redundant self-explanatory comment
const name = "test"; // set name to test